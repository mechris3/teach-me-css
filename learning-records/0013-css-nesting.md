# CSS nesting understood

The user completed Lesson 12 covering: nesting as syntactic sugar (no new capabilities), & as parent selector reference, compound vs descendant selectors (space vs no space — the user asked for explicit clarification on this), nesting media/container/supports queries for co-location, specificity equivalence with flat selectors, the deep-nesting trap, and CSS vs Sass nesting comparison.

## Evidence
- Asked to clarify the space vs no-space distinction in selectors (.card .title vs .card.title) — understood once explained. This is fundamental selector syntax that needed reinforcing.
- Correctly identified that nesting is "just syntactic sugar" and that media query co-location is its most valuable feature. Shows good prioritisation of what matters vs what's flashy.
- Asked what @supports is — now understands feature queries.

## Implications
- Selector fundamentals (combinators, compound selectors) may need a dedicated lesson. The space/no-space question suggests the user would benefit from a comprehensive selectors lesson.
- Ready for: modern selectors (:has, :is, :where, :not), or @scope, or view transitions.
