# CSS value functions understood

The user completed Lesson 3 covering: calc() (arithmetic, mixed units, spacing rules), min() (upper bound), max() (lower bound), clamp() (floor/preferred/ceiling), and color-mix() (blending colours in different spaces). Also covered colour spaces in depth — sRGB, HSL, OKLab, OKLCH — with examples and when to use each.

## Evidence
Completed lesson. Asked for expanded colour space coverage, indicating genuine interest in understanding the "why" not just the syntax.

## Implications
- Ready for @function (custom CSS functions) — the natural next step since it builds on all three previous lessons (custom properties + types + value functions).
- The user values understanding underlying models (colour spaces, mental models for min/max) not just syntax. Keep that depth.
