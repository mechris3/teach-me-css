# Anchor positioning understood

The user completed Lesson 17 covering: anchor-name and position-anchor, the anchor() function (reading as "my edge = their edge"), position-area 3×3 grid (all 9 positions including center center), the draggable demo (CSS tracks automatically), position-try-fallbacks (flip-block, flip-inline, custom @position-try), z-index/stacking context gotcha, the Popover API in detail (attributes, top layer, light dismiss, JS API, CSS styling), combining popover + anchor positioning, dynamic anchors in lists (shared popover pattern, Angular binding, HTML anchor attribute).

## Evidence
- Asked about dynamic anchor names in repeated lists — correctly identified the uniqueness constraint and engaged with the shared-popover-with-re-anchoring pattern.
- Asked about z-index and stacking contexts — correctly recalled that z-index is scoped to stacking contexts. Understood the popover/top-layer solution.
- Noted that anchor positioning replaces significant JS (Popper.js/Floating UI category).
- Connected it to the broader trend of CSS absorbing presentation logic from JS.

## Implications
- Anchor positioning + popover deeply understood, including Angular integration patterns.
- Remaining mission topics: @scope, advanced colour (oklch, relative colour syntax).
- The user finds "things that replace JS" particularly engaging — continue prioritising features with that quality.
