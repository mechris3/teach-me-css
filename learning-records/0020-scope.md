# @scope understood

The user completed Lesson 19 covering: @scope basics (scoping to a subtree), the "to" limit clause (lower boundary / donut model), proximity-based cascade resolution, :scope pseudo-class, comparison with Angular ViewEncapsulation/BEM/Shadow DOM, and practical patterns (nested component isolation, theme regions).

## Evidence
- Connected @scope to Angular's ViewEncapsulation — correctly identified the parallel (same goal, different mechanism) and the limitations Angular's approach has (no lower boundary, no proximity).
- Observed that modern CSS features shift more styling from component CSS to global CSS — good architectural insight.
- Asked about BEM — understands it as a naming convention that's less necessary with native scoping.

## Implications
- All original mission topics complete. 19 lessons covering the full modern CSS landscape.
- User expressed interest in: theming deep-dive, selectors deep-dive, capstone project.
- The shift toward "global design system + light components" is a key architectural insight to reinforce.
