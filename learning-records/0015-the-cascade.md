# The complete cascade algorithm understood

The user completed Lesson 14 covering: the full resolution order (origin/importance → inline → layers → specificity → scope proximity → source order → inheritance), how specificity is calculated (the three-column scoring system), inheritance vs non-inheritance (text vs box mental model), controlling inheritance (inherit/initial/unset/revert/revert-layer), and a worked example walking through the cascade step by step.

## Evidence
Asked for detailed explanation of the specificity scoring notation (A, B, C). Understood once the column-per-category model was explained. This was a gap in fundamental knowledge that's now filled.

## Implications
- The user now has the complete CSS mental model: values + types + functions + layout + motion + cascade + selectors.
- Remaining topics from the mission: @scope, view transitions, logical properties, advanced colour.
- Specificity explanation has been backported to Lesson 11 where it logically belongs.
