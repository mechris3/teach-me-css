# Advanced colour understood

The user completed Lesson 18 covering: oklch (perceptual lightness, chroma, hue), the colour wheel and hue angles, HSL vs oklch perceptual comparison, wide-gamut colours (P3, gamut mapping), relative colour syntax (deriving palettes from one token), light-dark() function, how color-scheme drives light-dark(), the theme toggle mechanism (class swap on root → color-scheme override → all light-dark() calls respond), three theming approaches (system-only, system+toggle, scoped via container style queries), and the recommended token architecture.

## Evidence
- Asked for the colour wheel to be shown as an actual circle — engaged with the visual model.
- Asked how light-dark() knows which value to pick — needed the color-scheme resolution chain explained explicitly.
- Asked about alpha channel support in oklch — understood the /alpha syntax.
- Identified theming as a topic deserving its own lesson — correct instinct.

## Implications
- Colour system deeply understood. Can build oklch-based palettes with relative colour syntax.
- Theming lesson identified as future topic (multi-brand, token hierarchy, Angular integration).
- Remaining original mission topic: @scope.
- Additional future topics: theming deep-dive, possibly a capstone project.
