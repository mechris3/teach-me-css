# CSS Animations understood

The user completed Lesson 7 covering: @keyframes (from/to and percentage steps), the animation shorthand (all 8 sub-properties), fill-modes (with timeline diagram explaining "both"), direction (normal/reverse/alternate), pause/play and prefers-reduced-motion, scroll-driven animations (scroll() with scroller and axis arguments, view() with animation-range), multiple animations on one element, and practical patterns (staggered entrance, spinners, pulse).

## Evidence
- Asked for clarification on fill-mode "both" — needed the timeline explanation to understand it applies to different time windows, not simultaneously. Good sign of deep reasoning.
- Initially confused by view() demo because scrolled the wrong container — but then correctly identified the behaviour. Shows understanding of which scrollport view() tracks.

## Implications
- Motion toolkit complete: transitions + animations + scroll-driven.
- Ready for layout (Flexbox/Grid), cascade layers (@layer), nesting, or container queries.
- User appreciates rendered demos for every code sample — maintain this standard in future lessons.
