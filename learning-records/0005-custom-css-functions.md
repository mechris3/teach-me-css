# CSS @function understood

The user completed Lesson 4 covering: the @function at-rule, syntax (--name, parameters as custom properties, result descriptor), typed parameters with defaults, local variables, function composition (calling functions from functions), conditional logic with @media and if() inside functions, the "no early returns / last result wins" rule, curly-brace syntax for comma-containing arguments, comparison with Sass @function, and current limitations (no loops, no recursion, no string manipulation).

## Implications
- The user now has the full "custom properties → types → value functions → custom functions" stack.
- Ready to move into layout (Flexbox/Grid), animations, or advanced attr().
- Can reference @function patterns in future lessons without re-explaining.
