# Custom properties fundamentals covered

The user has completed Lesson 1 covering: declaration/usage syntax, DOM-based scoping vs Sass compile-time resolution, :root as global scope, fallbacks with var(), the "invalid value" subtlety, runtime reactivity via JS, naming conventions, and the can't-concatenate gotcha (use calc() instead).

## Implications
- Ready for Lesson 2: @property (registered custom properties) — type system, inheritance control, and animating custom properties.
- Can reference var(), fallbacks, and scoping concepts without re-explaining them.
